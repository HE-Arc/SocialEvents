<% content_for :javascript_includes do %>
  <script src="//maps.google.com/maps/api/js?v=3.13&sensor=false&libraries=geometry" type="text/javascript"></script>
  <script src="//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js" type="text/javascript"></script>
  <%= raw javascript_include_view_js %>
<% end %>

<div id="dialog-cover" title="<%= @event.title %>" style="display: none;">
  <%= image_tag(@event.picture, :alt => @event.title) %>
</div>

<div id="dialog-cover-opener" class="image-cover-div pointer" title="<%= @event.title %>">
  <%= image_tag(@event.picture, :alt => @event.title ,:class => "img-cover") %>
</div>

<section class="show">
  <div class="show-body">
    <div class="show-title">
      <div class="title-complement"><%= link_to "Voir sur Facebook", 'https://www.facebook.com/' + @event.id_facebook, :target => '_blank' %></div>
      <p class="title-event"><%= @event.title %></p>
      <p class="date-event">From <%= @event.start_time.strftime("%d %B %Y") %> to <%= @event.end_time.strftime("%d %B %Y") %></p>
    </div>
    <div class="show-content">
      <div class="show-information">
        <div>
          <label>Category:</label> 
          <%= @event.category %>
        </div>
        <div>
          <label>Localisation:</label>
          <%= @event.event_location.street %>, <%= @event.event_location.city %>, Canton <%= @event.event_location.canton %>
        </div>
        <div>
          <label>Organizer:</label> 
          <%= link_to @event.event_location.name, @event.event_location.link, :target => "_blank" %>
        </div>
        <div>
          <label>Published by user:</label>
          <%= link_to @user.first_name + " " + @user.last_name, @user %>
        </div>
      </div>
      <div class="show-description">
        <pre><%= @event.description %></pre>
      </div>
    </div>
  </div>

    <div class="map">
      <%= hidden_field_tag 'lat', @events_location.latitude %>
      <%= hidden_field_tag 'lon', @events_location.longitude %>
      <div id="map" style='width: 100%; height: 400px;'></div>
    </div>
</section>